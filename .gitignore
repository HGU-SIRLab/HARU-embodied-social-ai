# ROS2 빌드 산출물
build/
install/
log/
src/build/
src/install/
src/log/

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg

# 대용량 바이너리 / 설치 파일
*.whl
*.whl.*
*.deb

# ============================================================
# HARU 런타임 데이터 (대용량 / 개인정보 / git에 넣지 않음)
# ============================================================

# HITL 수집 에피소드 — 이미지+모터값, 에피소드당 수백MB 가능
data/episodes/

# 학습된 LoRA 어댑터 — .safetensors, 에피소드당 80~200MB
data/adapters/

# 세션 장기 기억 (SWM) — 개인 대화 이력, 프라이버시 보호
data/memory/

# 학습·에피소드 관련 대용량 파일
*.npz
*.safetensors
*.bin
*.pt
*.ckpt

# ============================================================
# Python 가상환경
# ============================================================

# 프로젝트 전용 venv (apt/pip 설치본, 2~5GB)
haru_vla_env/

# 일반 venv 이름 변형
env/
.venv/

# ============================================================
# HuggingFace 캐시 (모델 다운로드 경로)
# ============================================================
# 홈 디렉터리 기준이지만 만약 프로젝트 폴더에 심볼릭 링크로 연결했을 경우
.cache/

# ============================================================
# 구버전 디렉터리
# ============================================================

# Phase 1~3 OpenVLA / 단일 VLA 코드 잔재
vla/

# torchvision 빌드 소스 (대용량, 커밋 불필요)
torchvision_src/

# TRT-LLM 변환 엔진 (대용량 바이너리)
data/trtllm/

# auto_round 양자화 모델 (대용량, 로컬 생성)
data/gemma4_autoround_w4a16/

# ============================================================
# 테스트 파일
# ============================================================
sample_voice.wav
*.wav
*.mp3

# ============================================================
# IDE / OS 파일
# ============================================================
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db

# ============================================================
# 환경 변수 / 비밀 정보
# ============================================================
.env
.env.*
secrets.yaml
